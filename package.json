{
  "name": "home-gym-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first home gym workout tracker — vanilla HTML/CSS/JS, no build step.",
  "scripts": {
    "start": "npx --yes serve .",
    "dev": "npx --yes serve ."
  }
}
