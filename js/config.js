/* ============================================================
   SYNC CONFIG
   ------------------------------------------------------------
   Paste the URL of your deployed sync backend below. Pick ONE
   backend from the /backends folder and follow its README:
     • backends/deno       (Deno Deploy — sign in with GitHub)   ← recommended
     • backends/netlify     (Netlify — sign in with GitHub)
     • backends/cloudflare  (Cloudflare Workers)
   Leave it as "" to keep the app fully local — the Sync panel
   will just show setup instructions.

   Example:  window.SYNC_API = "https://home-gym-sync.YOURNAME.deno.dev";
   (No trailing slash, no /data — sync.js adds that.)
   ============================================================ */
window.SYNC_API = "";
